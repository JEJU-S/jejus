<!DOCTYPE html>
<html>
    <head>
        <meta charset = "UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" href="https://unpkg.com/mvp.css">
        <title> Profile | Sample </title>
    </head>
    <body>
        <header>
            <img src="" alt="logo">
        </header>
        <div class="sidebar">
            <div class="side-profile">
                <img src="<%=user.image_url%>" alt="user photo"/>
                    <p><%=user.name%></p>
                    <p><%=user.gmail%></p>
            </div>
            <ul> <!--유저 플랜 목록 function 해야 함/ 여기도 div 로 묶을까..?-->
                <li> plan 1🚔</li>
                <li> plan 2🛫</li>
        </div>
        <main>
            <div class="main-profile">
                <form action="", mdthod="POST">
                    <input type="url", name="image_url", placeholder="프로필 사진", value="<%=user.image_url%>">
                    <input type="text", name="name", placeholder="닉네임", value="<%=user.name%>">
                    <p>gmail : <%=user.gmail%></p><!--진회색으로 표시?-->
                    <input type="submit" value="수정하기">
                </form>
            </div>
        </main>
            <footer>
                &copy; <%=new Date().getFullYear()%> Sample Project
            </footer>
    </body>
</html>